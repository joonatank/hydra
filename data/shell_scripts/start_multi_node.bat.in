REM Add necessary dlls to path
Path = %Path%;${LIB_DIR}\bin;${LIB_DIR}\bin\${BUILD_TYPE};${LIB_DIR};${LIB_DIR}\OGRE\bin\${BUILD_TYPE};${LIB_DIR}\CEGUI\bin\${BUILD_TYPE};${LIB_DIR}\expat\bin\${BUILD_TYPE}

REM Start listening client
start ${PROGRAM_NAME} --slave right --server ${SERVER_HOSTNAME}:${SERVER_PORT}
REM Start the master
${PROGRAM_NAME} ^
 -e ${DEMO_DATA_PATH}/multi_node.env ^
 -p ${DEMO_DATA_PATH}/${PROJ_PATH_RELATIVE} ^
 -g ${DEMO_DATA_PATH}/global/global_config.prj ^
 -v %*

