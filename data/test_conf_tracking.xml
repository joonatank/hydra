<?xml version="1.0"?>
<config>
	<!-- Setting paths -->

	<!-- Root path which can be referenced later and made paths relative to. -->
	<root name="data" path="${VL_DATA_PATH}" />
	<root name="media" path="${VL_MEDIA_PATH}" />

	<!-- Ogre plugins file, only one of these is supported,
	root is a reference to the directory containing this file.
	No root need to be present but then the file is assumed to be in the
	directory where the program is started in.
	-->
	<plugins root="data" >plugins.cfg</plugins>

	<!-- Equalizer VR configuration, only one of these is supported -->
	<eqc root="data" >eqc/1-window.eqc</eqc>

	<!-- Ogre resource file, multiple of these can be present -->
	<resources root="data" >resources.cfg</resources>
	<resources root="media" >resources.cfg</resources>

	<!-- Scene files to load. Should be in Ogre resource path to be loaded.
	Name is the reference used inside the program. Can be empty.
		If empty name is automatically generated and it's not possible to
		reference the object later, for example unload it.
	File is the resource name used by Ogre. Can not be empty.
		Multiple files can exist, they are all loaded under SceneNode
		named by name.
	Attach is to what SceneNode to attach the scene file. Can be empty.
	Type can be debug, release or all. If it's empty all is assumed.
		Mode the program is running can be control at start or at run time.
	-->
	<scene name="T7"> 
		<file>T7.scene</file>
	</scene>
	<scene name="test">
		<file>test.scene</file>
		<attach>cave</attach>
	</scene>
	<!-- Load debug lighting schema -->
	<scene name="work_lighting" >
		<type>debug</type>
		<file>work_lighting.scene</file>
		<attach></attach>
	</scene>
	<!-- Load release lighting schema -->
	<scene name="lighting" >
		<type>release</type>
		<file>lighting.scene<file>
	</scene>

	<!--
	Server configuration, currently only supports vrpn servers.
	vrpn trackers are of form <tracker>@<host>:<port>
	and sensor if that tracker has more than one of them, 
	if no sensor is provided it's assumed to mean the first sensor.
	-->
	<server type="vrpn" >
		<host>kosu-vr6.me.tut.fi</host>
		<!-- Port defaults to 3883, vrpn default --> 
		<port />

		<tracker name="glasses">
			<!-- Some parameters to transform the tracker values
			For vector as well as quaternion both attribute and element styles
			are valid.
			Any of these elements can be missing and they are assumed to be
			default values.
			-->
			<permute>
				<vector x=0 y=1 z=2 />
			</permute>
			<sign>
				<vector x=1 y=1 z=1 />
			</sign>
			<scale>
				<vector>
					<x>1</x>
					<y>1</y>
					<z>1</z>
				</vector>
			</scale>

			<!--
			Map the tracker e.g. the first sensor to an observer
			i.e. human moving in VR space. Observer without a name will match
			to all observers useful if you only have one VR system.
			-->
			<map type="Observer" name="" />
			<!-- Mapping to camera that can be used to visualize the
			observer to others. -->
			<map type="SceneNode" name="VisualCam" />
		</tracker>

		<!-- Mevea Toro T7 tracker has multiple sensors which are mapped to
			 SceneNodes. -->
		<tracker name="Mevea">
			<sensor num=0>
				<!-- Map sensor to a SceneNode in the scene using specific name -->
				<map type="SceneNode" name="eturunko" />
			</sensor>
			<sensor num=1>
				<map type="SceneNode" name="takarunko" />
				<!-- Multiple mapping is allowed -->
				<map type="SceneNode" name="keskirunko" />
			</sensor>
		</tracker>
	</server>

</config>
